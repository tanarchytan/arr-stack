---
# Docker Integration Configuration
# This allows Homepage to show container stats

# Docker socket integration (already mounted in docker-compose.yml)
# Shows container status, CPU, memory usage

# No additional configuration needed - Homepage will auto-detect
# containers on the media_internal network
